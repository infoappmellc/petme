# Copy this file to .env.local and provide your configuration
ADMIN_TOKEN=123123
# Point these to the deployed API origin (e.g. your Cloudflare Worker URL).
# For local development you can start a proxy or custom backend and update them accordingly.
API_BASE_URL=https://petme-backend.yourdomain.workers.dev
NEXT_PUBLIC_API_BASE_URL=https://petme-backend.yourdomain.workers.dev
